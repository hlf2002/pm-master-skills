---
name: pm-prototype-generator
description: 草图转交互原型生成器。产品经理上传手绘草图，自动识别UI元素并生成高保真可交互的HTML/CSS/JS原型。用于：PM画完草图后生成可演示的交互原型、快速验证设计方案。
---

# PM 原型生成器

将手绘草图转换为高保真交互原型的技能。

## 工作流程

### 第一步：产品分析（必做）
**在生成任何原型之前，必须先完成产品分析报告**

当用户描述项目背景或提供产品官网/文档时，必须先形成产品分析报告，包含：

1. **目标用户与需求**
   - 产品服务谁
   - 解决什么痛点
   - 核心需求是什么

2. **解决方案**
   - 产品的核心功能逻辑
   - 用户使用路径

3. **商业模式**
   - 如何赚钱（会员/付费/广告等）

4. **增长方式**
   - 获客策略（社交裂变/内容营销/投放等）

5. **竞争壁垒**
   - 为什么难以被复制

**只有完成产品分析并与用户确认后，才能进入原型设计阶段**

---

### 第三步：接收草图
用户会上传一张手绘的原型草图（照片或截图）。用 Read 工具查看图片内容。

### 第四步：识别UI元素
分析草图中的所有UI组件，包括：
- 导航栏 / 标签栏
- 按钮（主按钮、次按钮、图标按钮）
- 输入框（文本输入、搜索框、表单输入）
- 列表项（文字列表、图片+文字列表）
- 图片区域
- 卡片/容器
- 底部导航/侧边栏
- 图标
- 文字标签/标题

识别每个元素的位置（顶部/中部/底部）、尺寸、样式特征。

### 第五步：生成原型代码
调用 frontend-design skill 生成高保真HTML原型：

1. **设计风格选择**：
   - 现代简洁风（适合B端产品）
   - 卡片式设计（适合移动端）
   - 数据仪表盘（适合后台产品）
   - 电商风格（适合零售类）

2. **代码要求**：
   - 单HTML文件，包含内联CSS和JS
   - 使用现代CSS（Flexbox/Grid）
   - 添加基础交互（按钮点击、输入反馈、页面切换）
   - 响应式适配移动端

### 第六步：输出结果
生成后：
1. 在浏览器中打开预览
2. 告诉用户如何预览和分享

## 交互示例

常见可添加的交互：
- 按钮hover/点击效果
- 输入框focus状态
- 列表项点击（可跳转模拟）
- 标签页切换
- 模态框弹出
- 下拉菜单
- 加载状态模拟

## 模板参考

详见 `templates/` 目录下的模板文件。

## 注意事项

- 草图不需要很精美，潦草的线条也可以识别
- 如果草图中有文字标注，优先使用标注内容
- 如果不确定某个元素是什么用途，生成后让用户确认并调整
- 确保生成的代码可以直接在浏览器打开使用
